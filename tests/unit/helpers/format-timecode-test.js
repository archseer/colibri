import { formatTimecode } from '../../../helpers/format-timecode';
import { module, test } from 'qunit';

module('Unit | Helper | format timecode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTimecode(42);
  assert.ok(result);
});
